{
  "$schema": "https://schema.tauri.app/capability/2",
  "identifier": "default",
  "description": "DBan 主窗口能力",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "store:default",
    "dialog:allow-open",
    "autostart:allow-enable",
    "autostart:allow-disable",
    "autostart:allow-is-enabled"
  ]
}
